import { Grid, Typography } from '@mui/material';

const ContactHero = () => (
  <Grid container item size={{xs:12, md:6}} justifyContent="center">
    <Typography variant="h3" fontWeight="bold">CONTACT US</Typography>
    <Typography mt={1}>We’re here to help. Get in touch with any questions or comments!</Typography>
  </Grid>
);

export default ContactHero;



